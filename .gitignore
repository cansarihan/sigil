# Rust
target/
**/*.rs.bk

# Regenerated on every `cargo test`; useful locally for debugging a failing
# test, but 500 KB of churn in every diff if committed.
test_snapshots/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Env & secrets — never commit keys
.env
.env.*
!.env.example
*.key
secrets/

# Build artifacts
*.wasm
!docs/artifacts/*.wasm

# OS
.DS_Store
